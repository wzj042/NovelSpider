 # content = subReplace('[a-zA-Z]+\n+\s*[a-zA-Z]+', content, {
    #     '\n+\s*':''
    # })
    # content = subReplace(notEndSymbol+'?'+'\n+\s*'+cn, content, {
    #     '\n+\s*':''
    # })
    # content = subReplace(notEndSymbol+'?'+'\n+\s*'+endSymbol, content, {
    #     '\n+\s*':''
    # })
    # content = subReplace(cn+'?'+'\n+\s*'+symbol, content, {
    #     '\n+\s*':''
    # })